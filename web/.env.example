# CockroachDB connection string (Postgres wire-compatible).
# Get it from the CockroachDB Cloud console: cluster -> Connect.
# Example: postgresql://user:password@host:26257/defaultdb?sslmode=verify-full&options=--cluster%3Dyour-cluster-name
DATABASE_URL=
